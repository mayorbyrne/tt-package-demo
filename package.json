{
  "name": "@mayorbyrne/tt-package-demo",
  "version": "1.0.0",
  "description": "A demo package for Total Typescript",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "ci": "npm run build && npm run check-format && npm run check-exports && npm run test",
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "check-exports": "attw --pack . --ignore-rules=cjs-resolves-to-esm",
    "test": "vitest run",
    "dev": "vitest",
    "local-release": "changeset version && changeset publish",
    "prepublishOnly": "npm run ci"
  },
  "keywords": [
    "demo",
    "typscript"
  ],
  "homepage": "https://github.com/mayorbyrne/tt-package-demo",
  "bugs": {
    "url": "https://github.com/mayorbyrne/tt-package-demo/issues"
  },
  "author": "Kevin Moritz",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mayorbyrne/tt-package-demo.git"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.15.4",
    "@changesets/cli": "^2.27.8",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "main": "dist/index.js"
}
